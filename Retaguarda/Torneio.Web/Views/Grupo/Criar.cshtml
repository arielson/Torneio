@model Torneio.Application.DTOs.Grupo.CriarGrupoDto
@{ ViewData["Title"] = "Novo Grupo"; }

<div class="row justify-content-center">
    <div class="col-md-6">
        <h4 class="mb-3"><i class="bi bi-plus-circle me-2"></i>Novo Grupo</h4>

        <form asp-action="Criar" asp-route-slug="@ViewBag.Torneio?.Slug" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="alert alert-danger small" style="display:none"></div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Nome do grupo *</label>
                <input asp-for="Nome" class="form-control" placeholder="Ex.: Equipe Alpha" autofocus />
                <span asp-validation-for="Nome" class="text-danger small"></span>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-lg me-1"></i>Salvar
                </button>
                <a asp-action="Index" asp-route-slug="@ViewBag.Torneio?.Slug" class="btn btn-outline-secondary">
                    Cancelar
                </a>
            </div>
        </form>
    </div>
</div>
