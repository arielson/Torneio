@model IEnumerable<EquipeDto>
@{
    var slug       = Context.GetRouteData().Values["slug"]?.ToString();
    var anoId      = Context.GetRouteData().Values["anoId"]?.ToString();
    var ano        = (AnoTorneioDto)ViewBag.Ano;
    var torneio    = (TorneioDto)ViewBag.Torneio;
    var label      = torneio?.LabelEquipe ?? "Equipe";
    var labelMembro = torneio?.LabelMembro ?? "Membro";
    ViewData["Title"] = $"{label}s — {ano.Ano}";
    var lista = Model.ToList();
}

<div class="page-header">
    <div class="page-header-left">
        <h1>@label&#x73; — @ano.Ano</h1>
    </div>
    <div class="page-header-actions">
        <a href="/@slug/equipes/@anoId/criar" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-lg me-1"></i>Nova @label
        </a>
    </div>
</div>
<a href="/@slug/admin" class="back-link">
    <i class="bi bi-arrow-left"></i> Painel
</a>

<div class="content-card">
    <div class="card-header-bar">
        <h5><i class="bi bi-people me-2"></i>@label&#x73; cadastradas</h5>
    </div>
    @if (!lista.Any())
    {
        <div class="card-body-pad text-muted">
            Nenhuma @label.ToLower() cadastrada. <a href="/@slug/equipes/@anoId/criar">Criar a primeira</a>.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Capitão</th>
                        <th>Vagas</th>
                        <th>@labelMembro&#x73;</th>
                        <th class="text-end">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var e in lista)
                    {
                        <tr>
                            <td class="fw-semibold">@e.Nome</td>
                            <td>@e.Capitao</td>
                            <td>@e.QtdVagas</td>
                            <td>
                                <span class="@(e.QtdMembros >= e.QtdVagas ? "text-success fw-semibold" : "")">
                                    @e.QtdMembros / @e.QtdVagas
                                </span>
                            </td>
                            <td class="text-end">
                                <div class="d-flex gap-1 justify-content-end flex-wrap">
                                    <a href="/@slug/equipes/@anoId/@e.Id/membros" class="btn btn-sm btn-outline-secondary" title="@labelMembro&#x73;">
                                        <i class="bi bi-person me-1"></i>@labelMembro&#x73;
                                    </a>
                                    <a href="/@slug/equipes/@anoId/@e.Id/editar" class="btn btn-sm btn-outline-secondary" title="Editar">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <form asp-controller="Equipe" asp-action="Remover" asp-route-slug="@slug" asp-route-anoId="@anoId" asp-route-id="@e.Id" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button class="btn btn-sm btn-outline-danger" onclick="return confirm('Remover @e.Nome?')" title="Remover">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
